# Environment variables for tenant-service are in the monorepo root .env.example
# See: ../.env.example
#
# Relevant vars for this service:
#   NODE_ENV, LOG_LEVEL, CORS_ORIGINS
#   FIREBASE_SERVICE_ACCOUNT_JSON
#   TENANT_SERVICE_PORT (default: 4002)
