# Environment variables for scripts/ are in the monorepo root .env.example
# See: ../.env.example
#
# The seed script reads from the root .env directly:
#   pnpm seed   (runs: tsx --env-file=.env scripts/seed.ts)
#
# Relevant vars:
#   FIREBASE_SERVICE_ACCOUNT_JSON
#   FIREBASE_DATABASE_URL
